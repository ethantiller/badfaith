{
  "name": "badfaith-extension",
  "version": "0.1.0",
  "description": "Bad Faith - News Article Auditing Browser Extension",
  "type": "module",
  "scripts": {
    "dev": "rm -rf dist && sh -c 'vite build --watch & BUILD_TARGET=content vite build --watch & BUILD_TARGET=background vite build --watch & wait'",
    "build": "rm -rf dist && npm run typecheck && vite build && BUILD_TARGET=content vite build && BUILD_TARGET=background vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.234",
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
